name: LCXX build & test - Windows

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]

defaults:
  run:
    shell: bash

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive

    - name: install OpenSSL
      run: choco install openssl

    - name: install boost
      run: choco install boost-msvc-14.3

    - name: configure
      run: |
        cmake -DCMAKE_BUILD_TYPE=Release -DBOOST_ROOT="C:/local/" -DBoost_INCLUDE_DIRS="C:/local/boost_1_80_0" -S . -B build ..

    - name: build
      run: cmake -B build --build . --config Release 

    - uses: actions/upload-artifact@master
      with:
        name: lcxx-tests-windows
        path: |
          ${{ github.workspace }}/build/tests/*/
        retention-days: 3
        if-no-files-found: error

  tests-windows:
    needs: build-windows
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: install OpenSSL
      run: choco install openssl

    - uses: actions/download-artifact@v3
      with:
        name: lcxx-tests-windows

    - name: display artifacts
      run: ls -R

    - name: crypto-tests
      run: ./crypto/Release/crypto_test.exe

    - name: lcxx-tests
      run: ./lcxx/Release/lcxx_test.exe